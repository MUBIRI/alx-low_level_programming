#include "main.h"
/**
 * _abs - computes absolute value of an int
 * @j: int to check
 * Return: absolute value of int
 */
int _abs(int j)
{
	if (j >= 0)
	{
		return (j);
	}
	return (-j);
	_putchar('\n');
}
